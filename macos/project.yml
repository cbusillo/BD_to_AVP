name: BluRayToVisionPro

options:
  createIntermediateGroups: true
  defaultConfig: Debug
  deploymentTarget:
    macOS: "26.0"
    visionOS: "26.0"

configs:
  Debug: debug
  Release: release

packages:
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle
    exactVersion: 2.9.4

settings:
  base:
    CLANG_ENABLE_MODULES: YES
    MACOSX_DEPLOYMENT_TARGET: "26.0"
    SWIFT_VERSION: "5.0"

targets:
  BluRayToVisionPro:
    type: application
    platform: macOS
    sources:
      - path: BluRayToVisionPro
      - path: ../bd_to_avp/resources/app_icon.icns
        buildPhase: resources
      - path: ../bd_to_avp/resources/iso639_languages.json
        buildPhase: resources
    dependencies:
      - package: Sparkle
    settings:
      base:
        CODE_SIGN_STYLE: Automatic
        CURRENT_PROJECT_VERSION: 146
        BD_TO_AVP_SUPPORT_DIAGNOSTICS_ENDPOINT: ""
        ENABLE_APP_SANDBOX: NO
        ENABLE_HARDENED_RUNTIME: YES
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: BluRayToVisionPro/Info.plist
        MARKETING_VERSION: 0.2.143
        PRODUCT_BUNDLE_IDENTIFIER: com.shinycomputers.bd-to-avp
        PRODUCT_MODULE_NAME: BluRayToVisionPro
        PRODUCT_NAME: 3D Blu-ray to Vision Pro
      configs:
        Debug:
          PRODUCT_BUNDLE_IDENTIFIER: com.shinycomputers.bd-to-avp.development
          PRODUCT_NAME: 3D Blu-ray to Vision Pro Development
        Release:
          ARCHS: arm64
          COPY_PHASE_STRIP: YES
          DEAD_CODE_STRIPPING: YES
          DEPLOYMENT_POSTPROCESSING: YES
          ENABLE_CODE_COVERAGE: NO
          INFOPLIST_FILE: BluRayToVisionPro/Info-Release.plist
          ONLY_ACTIVE_ARCH: NO
          OTHER_SWIFT_FLAGS: $(inherited) -file-prefix-map $(SRCROOT)/BluRayToVisionPro=/Sources -debug-prefix-map $(SRCROOT)/BluRayToVisionPro=/Sources -coverage-prefix-map $(SRCROOT)/BluRayToVisionPro=/Sources
          SWIFT_SERIALIZE_DEBUGGING_OPTIONS: NO

  BluRayToVisionProTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: BluRayToVisionProTests
    dependencies:
      - target: BluRayToVisionPro
    settings:
      base:
        BUNDLE_LOADER: $(TEST_HOST)
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.shinycomputers.bd-to-avp.tests
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/3D Blu-ray to Vision Pro Development.app/Contents/MacOS/3D Blu-ray to Vision Pro Development

  SpatialPlaybackProbe:
    type: application
    platform: auto
    supportedDestinations: [visionOS]
    deploymentTarget:
      visionOS: "26.0"
    sources:
      - path: SpatialPlaybackProbe
    settings:
      base:
        CODE_SIGN_STYLE: Automatic
        CURRENT_PROJECT_VERSION: 1
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: SpatialPlaybackProbe/Info.plist
        MARKETING_VERSION: 0.1.0
        PRODUCT_BUNDLE_IDENTIFIER: com.shinycomputers.bd-to-avp.spatial-playback-probe
        PRODUCT_MODULE_NAME: SpatialPlaybackProbe
        PRODUCT_NAME: BD to AVP Playback Check
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "7"
        XROS_DEPLOYMENT_TARGET: "26.0"

  SpatialPlaybackProbeTests:
    type: bundle.unit-test
    platform: auto
    supportedDestinations: [visionOS]
    deploymentTarget:
      visionOS: "26.0"
    sources:
      - path: SpatialPlaybackProbeTests
    dependencies:
      - target: SpatialPlaybackProbe
    settings:
      base:
        BUNDLE_LOADER: $(TEST_HOST)
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.shinycomputers.bd-to-avp.spatial-playback-probe.tests
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/BD to AVP Playback Check.app/BD to AVP Playback Check
        XROS_DEPLOYMENT_TARGET: "26.0"

  SpatialPlaybackProbeUITests:
    type: bundle.ui-testing
    platform: auto
    supportedDestinations: [visionOS]
    deploymentTarget:
      visionOS: "26.0"
    sources:
      - path: SpatialPlaybackProbeUITests
    dependencies:
      - target: SpatialPlaybackProbe
    settings:
      base:
        CODE_SIGN_STYLE: Automatic
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.shinycomputers.bd-to-avp.spatial-playback-probe.ui-tests
        TARGETED_DEVICE_FAMILY: "7"
        TEST_TARGET_NAME: SpatialPlaybackProbe
        XROS_DEPLOYMENT_TARGET: "26.0"

schemes:
  BluRayToVisionPro:
    build:
      targets:
        BluRayToVisionPro: all
        BluRayToVisionProTests: [test]
    test:
      gatherCoverageData: true
      targets:
        - BluRayToVisionProTests

  SpatialPlaybackProbe:
    build:
      targets:
        SpatialPlaybackProbe: all
        SpatialPlaybackProbeTests: [test]
    run:
      config: Debug
    test:
      targets:
        - SpatialPlaybackProbeTests
        - SpatialPlaybackProbeUITests
