# 0.3.3 Beta 2 Cut Packet

**Prepared metadata; publication pending.**

## Candidate Identity

- Owner: issue #741.
- Package version: `0.3.3b2`.
- Public version: `0.3.3-beta.2`.
- Tag and release name: `v0.3.3-beta.2`.
- Build: `173`.
- Route: `Prerelease` / Sparkle `beta`; GitHub prerelease; not Latest; PyPI and
  Homebrew publication disabled.

This candidate delivers Movie Sharing in the full **3D Blu-ray to Vision Pro**
Mac app for **Shiny 3D Cinema 0.1.0 (3)**. The Mac retains its conversion features
and established product identity. Testers choose completed movies on the headset
after approving a folder and pairing the devices. See the
[Movie Sharing beta quick start](movie-sharing-beta.md).

This is a later candidate in the `0.3.3` cycle. Its post-dispatch source SHA,
signed DMG and app-tree checksums, release run, release ID and appcast checksum
remain absent from preregistration. Record those facts only from the guarded
release's immutable receipts.

## Immutable Prior

Published Beta `0.3.3b1` / build `172` is the selected Beta updater source:

- tag and source SHA: `v0.3.3-beta.1` /
  `d9887254655c75c98373ab34a8c175af8a852c02`;
- Prerelease run: `33131418448`, attempt `1`;
- release ID: `378212647`, published `2026-08-28T01:32:59Z`;
- DMG SHA-256:
  `18e98863f5fc80078a79c467d7b71fd4d1e53d4ff9c01d1d0b206a3870e8d98f`;
- signed app-tree SHA-256:
  `88e567fdb37118b94ceb562a5717106af93d506f7a36a12067ac14903189cc83`;
- appcast SHA-256:
  `68927af8e7720e9753db1dc6ab9373334f084ce2e79f8258c36380e38bcabc0b`.

Its checked [release receipt](release-evidence/v0.3.3-beta.1/release-receipt.json)
and qualified Release Evidence v2 bundle remain immutable. Stable `0.3.2` /
build `171` remains the prior Stable history and evidence base. Burned builds
`147`, `154`, `165`, `166` and `168`, plus abandoned metadata build `157`, remain
unavailable for reuse.

## Production Preflight

Production Preflight run `34924807265`, attempt `1`, succeeded on exact protected
main `c87e3c8222efb9405691dfb924ef6b72acfa2563` before candidate preparation.
Both the actor and triggering actor were `shiny-code-bot`.

The retained artifact is `10380136226`, named
`production-preflight-success-c87e3c8222efb9405691dfb924ef6b72acfa2563-34924807265-1`,
with GitHub digest
`e7fb8747720036e7ad8925fd1593a49ac225e9836378e8e29e4b7ed58209f940`.
The source-bound preventive results were:

- DMG SHA-256:
  `1fa0676502222cabe2936ed8c9d5d87daee5dd120e7556f78285d50009192072`;
- DMG size: `167689728` bytes;
- app-tree SHA-256:
  `5db16dcd491d6c51938c31bb30626dba7c605e245919fd764e67bfa590778fd6`;
- installed UI evidence: `4` files / `156953` bytes;
- installed UI report SHA-256:
  `6e04e6c68c28966536e81a4e5e54d1003067d716197fb9db5f24646babfcc482`.

The package, embedded worker identity/protocol, profile persistence and update
controls passed. These are preventive results for the source commit, not signed
Beta 2 or publication evidence.

## Qualification And Delivery

`docs/qualification/v0.3.3-beta.2-signed-qualification-v1.json` preregisters the
closed 20-case matrix, current worker protocol and route-table digest. Its fresh
automated updater case is Beta 1 to Beta 2. Optional native presentation and
physical-device evidence retain the existing risk-based policy.

Preparation classification requires fresh change-scoped evidence for preview
cancellation/failure cleanup, generated final output, overwrite/conversion
cancellation, malformed PGS recovery and subtitle partial-output diagnostics.
Refresh those six cases on the prepared source before Prerelease dispatch,
using the documented `qualify/<beta-tag>` remediation branch. Prior evidence
cannot carry across the changed conversion and subtitle paths.

The owner confirmed Shiny 3D Cinema build 3 updates, retains the existing pairing
and plays stereo samples with Mac Current. The delivered signed Mac Beta still
needs its own updater and headset playback check. Broader measured headset work
remains in issue #739.

The candidate tag and authenticated release/draft listing were checked absent
before preparation. Recheck absence and exact protected main before dispatch.
Keep main fixed throughout the release. Monitor with
`uv run python -m scripts.github_release_run watch`; exit `20` requires fresh
explicit run-bound signing authorization. Approve only with the helper's exact
run ID, workflow, full main SHA, confirmation SHA and approval fingerprint.

After publication, use the immutable release and quick-start links in
TestFlight's beta description and review notes, then reconcile Release Evidence
and the automated signed-update results through the normal protected PR flow.
