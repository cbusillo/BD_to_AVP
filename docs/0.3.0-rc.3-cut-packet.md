# 0.3.0 RC 3 Cut Packet

> **Published and immutable.** RC 3 was published from protected-`main` SHA
> `0b06582a83a45bb38d851e62ccf38cd148c7bb95` by guarded Prerelease run
> `30990186667`. The release, tag, DMG, and appcast must not be replaced.

RC 3 follows immutable RC 2 and packages three post-RC2 changes: direct
accessibility for the profile save action from PR #466, malformed-PGS parser and
decoder hardening from PR #467, and privacy-safe subtitle progress and
partial-output diagnostics from PR #468. Issue #458 owns the exact signed-build
retest on the reporter's problematic source.

## Exact Metadata

| Field | Reviewed value |
| --- | --- |
| Package and bundle short version | `0.3.0rc3` |
| Public version | `0.3.0-rc.3` |
| Bundle and Sparkle build | `160` |
| GitHub tag and release title | `v0.3.0-rc.3` |
| DMG name | `3D-Blu-ray-to-Vision-Pro-0.3.0-rc.3.dmg` |
| Sparkle channel | `rc` |
| GitHub prerelease | `true` |
| GitHub Latest | `false` |
| Publish to PyPI or Homebrew | `false` |
| Privacy contract | Privacy rules version `5` |

Build `160` is globally newer than immutable RC 2 build `159`, RC 1 build
`158`, abandoned Beta 12 build `157`, and burned builds `147` and `154`. RC 3
is an immutable GitHub prerelease, is not Latest, and is available on the RC
Sparkle route.

## Reviewed Release-Note Seed

> BD_to_AVP `v0.3.0-rc.3` hardens PGS subtitle recovery and makes subtitle
> outcomes easier to diagnose. Malformed segments and image data are isolated
> without unbounded decoding, valid subtitle tracks can survive corrupt peers,
> and the app records aggregate subtitle progress in diagnostics while reporting
> partial-output warnings without exposing disc titles or paths. Profile save is
> also directly reachable by VoiceOver and other accessibility tools.

## Publication and Qualification

The exact public artifact is bound to its source SHA, successful guarded run,
immutable release, DMG, signed app tree, and live appcast. RC2-to-RC3 Install
and Relaunch, profile-save accessibility, malformed-PGS recovery, privacy-safe
subtitle diagnostics, network output, overwrite protection, and cancellation
passed. Three unchanged capacity/preview cases carried under the qualification
policy. The reporter confirmed the previously failing source succeeds with
subtitles enabled, and issue #458 is closed.

Content-aware targeted qualification derived expectations from the immutable
notes and matching appcast item. It found zero expected and zero observed issue
links because the immutable RC3 notes contain no issue URL; that category is not
applicable, not failed. All four PR links, the comparison link, and the
full-release link appeared as accessible external links and opened correctly.
This is an explicit source-bound decision, not a waiver or reinterpretation of
the immutable artifact. RC3 signed qualification is complete.

<!-- release-evidence-receipt-v1 -->
## Immutable Publication Receipt

- Source SHA: `0b06582a83a45bb38d851e62ccf38cd148c7bb95`
- Release workflow run: `30990186667` attempt `1`
- GitHub release ID: `365399671`
- Published at: `2026-08-05T09:06:41Z`
- DMG SHA-256: `e1d936cc3231aea4f9d87fde1fd9e7792c1189254fc85bfc10fea382ffce690f`
- Signed app-tree SHA-256: `30a7256feb4fb1dee78de70eefeec6a5e69db0e7355e7fd2aaf46efaa71f0d37`
- Appcast SHA-256: `6404424910172cfccc9f23472e8748eda7bfba5b1f07d373e69cdb9b0679e9ed`
- Generated receipt file SHA-256: `e2cd5c52b5e177048783a8ca2d8f6d8ba7580be8f933702a42072928839f2c3d`
- Targeted qualification receipt SHA-256: `d5547949a370bcb6d5484a59027f0a14961614a71b2e1df3f5bd2dcb3b7291ce`

The deterministic receipt was generated after publication because receipt
automation merged after RC3. The immutable release itself does not contain a
`release-receipt.json` asset; this checked-in receipt is a verified
post-publication backfill and does not claim otherwise.
