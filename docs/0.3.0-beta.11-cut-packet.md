# 0.3.0 Beta 11 Cut Packet

> **Historical prepublication record.** Beta 11 was published on August 2,
> 2026. The statements below preserve the metadata, qualification scope, and
> authorization boundary that applied before its guarded release dispatch.

The normative identity and route contract remains in
[release-routes.md](release-routes.md). Beta 11 advances from immutable
published Beta 10 and packages the post-Beta-10 preview-cleanup repair plus the
granular route-aware quality system. It is not an RC nomination.

## Exact Metadata

| Field | Reviewed value |
| --- | --- |
| Package and bundle short version | `0.3.0b11` |
| Public version | `0.3.0-beta.11` |
| Bundle and Sparkle build | `156` |
| GitHub tag and release title | `v0.3.0-beta.11` |
| DMG name | `3D-Blu-ray-to-Vision-Pro-0.3.0-beta.11.dmg` |
| Sparkle channel | `beta` |
| GitHub prerelease | `true` |
| GitHub Latest | `false` |
| Publish to PyPI or Homebrew | `false` |
| Product name | `3D Blu-ray to Vision Pro` |
| Bundle identifier | `com.shinycomputers.bd-to-avp` |
| Apple signing authority | `Developer ID Application: Shiny Computers Leasing LLC (MM5YXC7T6E)` |
| Feed, Sparkle key, diagnostics endpoint | Existing pinned production values |
| Privacy contract | Privacy rules version `4` |

Build `156` is globally newer than immutable Beta 10 build `155`; failed Beta
9 build `154` remains burned. The August 2, 2026 live audit confirmed that Beta
10 was published by guarded Prerelease run `30445073119` from protected-main
commit `50b874a4ad681762f3aa94e02926b8a82f0aa221`. Beta 11 has no tag,
release, draft, appcast item, or freeze entry. The public cumulative appcast
currently ends at Beta 10 build `155`.

## Included Product Changes

- PR #416 removes preview workspaces after GUI cancellation instead of leaving
  manual-cleanup residue on the selected destination.
- PRs #424 through #448 calibrate, implement, expose, and freeze mapping version
  `2` of the granular route-aware video-quality system.
- PR #449 validates every supported guided route through the packaged protocol-
  v12 worker and records the immutable package evidence used by #422.
- PRs #415, #417, and #423 include security and dependency maintenance merged
  after Beta 10.

The frozen quality behavior is direct MV-HEVC with seven checked guided steps
plus reversible Custom; generated MV-HEVC with only Balanced at `20 Mbps/eye +
merge 75` plus Custom; file upscale with Balanced `75` and Detailed `100`; and
AV1 as exact-CRF Custom only. Unsupported guided positions remain unavailable
rather than silently resolving to Balanced.

## Cumulative Update Contract

Publication must append Beta 11 above immutable public history while retaining
burned build `154` as absent:

| Order | Internal version | Build | Channel |
| ---: | --- | ---: | --- |
| 1 | `0.3.0b11` | `156` | `beta` |
| 2 | `0.3.0b10` | `155` | `beta` |
| 3 | `0.3.0b8` | `153` | `beta` |
| 4 | `0.3.0b7` | `152` | `beta` |
| 5 | `0.3.0b6` | `151` | `beta` |
| 6 | `0.3.0b5` | `150` | `beta` |
| 7 | `0.3.0b4` | `149` | `beta` |
| 8 | `0.3.0b3` | `148` | `beta` |
| 9 | `0.2.143` | `146` | Stable / no channel |
| 10 | `0.2.143rc5` | `145` | `rc` |
| 11 | `0.2.143rc4` | `144` | `rc` |

Stable and RC continue to exclude every Beta item. Beta and Alpha admit the
published Betas plus Beta 11 if publication succeeds. An installed Beta 10 may
update directly to build `156` without changing its saved route.

## Reviewed Release-Note Seed

> BD_to_AVP `v0.3.0-beta.11` adds granular route-aware video quality controls:
> seven calibrated direct MV-HEVC levels plus reversible Custom, evidence-backed
> subsets for generated MV-HEVC and file upscale, and exact-CRF Custom for
> experimental AV1. It also fixes preview cancellation cleanup on selected
> destination volumes. Beta 10 remains immutable published history, and failed
> Beta 9 build 154 remains omitted.

Do not describe Beta 11 as downloadable, signed, published, notarized, or
Sparkle-discoverable until the guarded workflow verifies the immutable public
state. Publishing the beta does not qualify AV1 on physical M5 Vision Pro and
does not turn the candidate into an RC.

## Exact-Artifact Qualification

The last exact public artifact is immutable Beta 10: version `0.3.0b10`,
protected-main SHA `50b874a4ad681762f3aa94e02926b8a82f0aa221`, guarded run
`30445073119`, DMG SHA-256
`6fed922114e152be4f2e95ad7ee597465ae8d550539e7566ed05a64d8176d91c`,
and signed app-tree SHA-256
`d89840da944b3a4519d68e84549ac7a69a9b2ffc5d2ec5717eabf6f0382151b0`.
It passed the release package smoke and proved successful real preview
presentation, but its exact GUI-cancellation run left preview-workspace residue.
PR #416 fixed that cleanup behavior after Beta 10. Beta 10 also predates the
protocol-v12 quality ladder and cannot qualify #422.

Beta 11 exact-artifact qualification is pre-registered in
[`beta11-shared-signed-qualification-v1.json`](qualification/beta11-shared-signed-qualification-v1.json).
The final receipt must bind one protected-`main` SHA to the signed and notarized
DMG, release/appcast identity, updater routes, all supported packaged
protocol-v12 quality mappings, low-space GUI preview, capacity-confidence
behavior, cleanup, final outputs,
overwrite/cancellation safety, and the public-safe #386 closure. Existing
mapping, package, physical Vision Pro, and full-length receipts remain valid
inputs and are not rerun merely to create a new candidate identity.

The workflow's built-in release smoke gates publication. The remaining field
matrix runs against the exact published beta artifact. If any field row fails,
Beta 11 remains immutable public history and the fix advances to a new beta
version and global build; assets, notes, tags, and appcast items are never
silently replaced.

## Authorization Boundary

This packet authorizes metadata preparation and review, not an unattended
release. Before dispatch, record the exact protected `main` SHA and establish a
temporary merge hold. While the Prerelease workflow is nonterminal, `main` must
remain fixed. Monitor only with
`uv run python -m scripts.github_release_run watch`. If it exits `20`, obtain
fresh explicit run-bound authorization in the active conversation and approve
only through `scripts.github_release_run approve` with the emitted fingerprint.
