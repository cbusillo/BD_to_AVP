# 0.3.0 Stable Cut Packet

> **Prepared metadata; publication pending.** Stable may be dispatched only
> from the exact merged protected-`main` SHA under a temporary merge hold. The
> `macos-signing` environment requires fresh explicit run-bound authorization
> in the active conversation.

Stable follows immutable RC 3 and promotes the complete `0.3.0` production
train tested across Alpha, Beta, and RC. GitHub-generated notes compare with the
previous Stable release `v0.2.143`, so they retain the complete user-facing
change set rather than only the RC3 delta. Issue #489 owns publication,
qualification, and closeout.

## Exact Metadata

| Field | Reviewed value |
| --- | --- |
| Package and bundle short version | `0.3.0` |
| Public version | `0.3.0` |
| Bundle and Sparkle build | `161` |
| GitHub tag and release title | `v0.3.0` |
| DMG name | `3D-Blu-ray-to-Vision-Pro-0.3.0.dmg` |
| Sparkle channel | absent (Stable default) |
| GitHub prerelease | `false` |
| GitHub Latest | `true` |
| Publish to PyPI | `true` |
| Downstream Homebrew update | `true` |
| Privacy contract | Privacy rules version `5` |

Build `161` is globally newer than immutable RC 3 build `160`, RC 2 build
`159`, RC 1 build `158`, and all earlier production history. Burned builds
`147` and `154` and abandoned metadata build `157` remain absent. Stable has no
tag, release, draft, PyPI version, Homebrew pull request, appcast item, or freeze
entry until guarded publication succeeds.

## Reviewed Release-Note Seed

> BD_to_AVP `v0.3.0` introduces the production SwiftUI app and its direct
> spatial-video conversion workflow. It adds native Blu-ray, disc-folder, MKV,
> and batch handling; calibrated MV-HEVC and AV1 output choices; audio and
> language controls; profiles, preview, progress, cancellation, and storage
> safety; Sparkle update routes and native release notes; and privacy-safe
> diagnostics. The Stable candidate also includes the RC hardening for
> malformed PGS subtitles, partial subtitle output, profile recovery, and
> accessibility.

## Qualification And Authorization

Immutable RC 3 is the exact prior artifact input. Preparation may carry only
ordinary Tier 2 cases whose mapped paths and contracts remain clean. The real
RC3-to-Stable Sparkle route, native release-note presentation, clean-machine
update, installed UI accessibility, USB Blu-ray/MakeMKV path, protected-media
conversion, and Vision Pro playback are preregistered for the post-publication
`milestone` phase because their exact evidence requires the published Stable
DMG, checked receipt, and live appcast. Missing physical hardware must remain an
honest skipped or unavailable record and never a fabricated pass.

Before dispatch, record the exact protected `main` SHA and establish a temporary
merge hold. Monitor only with `uv run python -m scripts.github_release_run
watch`. If it exits `20`, obtain fresh explicit run-bound authorization in the
active conversation and approve only through `scripts.github_release_run
approve` with the emitted run ID, workflow, full `main` SHA, confirmation SHA,
and approval fingerprint.
