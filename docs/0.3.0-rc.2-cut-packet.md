# 0.3.0 RC 2 Cut Packet

> **Published and immutable.** RC 2 was released from protected-main SHA
> `cd56f02bab8589f527af6e45fe94b2ffcce473dc` by guarded Prerelease run
> `30944931796`. Its tag, DMG, release, appcast item, and signed identity are
> production history and must not be rebuilt, replaced, or republished.

RC 2 follows immutable RC 1 and packages the malformed-PGS recovery hardening
for #458 from PR #463 and Files & Recovery profile persistence for #462 from PR
#464.

## Exact Metadata

| Field | Reviewed value |
| --- | --- |
| Package and bundle short version | `0.3.0rc2` |
| Public version | `0.3.0-rc.2` |
| Bundle and Sparkle build | `159` |
| GitHub tag and release title | `v0.3.0-rc.2` |
| DMG name | `3D-Blu-ray-to-Vision-Pro-0.3.0-rc.2.dmg` |
| Sparkle channel | `rc` |
| GitHub prerelease | `true` |
| GitHub Latest | `false` |
| Publish to PyPI or Homebrew | `false` |
| Privacy contract | Privacy rules version `5` |

Build `159` is globally newer than immutable RC 1 build `158`, abandoned Beta
12 build `157`, and burned builds `147` and `154`. RC 2 is immutable published
history; RC 3 is the next candidate.

Guarded Prerelease run `30944931796` published RC 2 at
`2026-08-04T20:28:42Z`. GitHub release ID `365132159` contains the
`409,258,291`-byte DMG with SHA-256
`e39e81b99cf9c7bd272095d7a3f96de378e0a251334e9a4c5a83c54b8f4c1d45`.
The signed app-tree SHA-256 is
`961b5e3bb0c2aba4b5ce474a0f0a559b80a037712df1f743427f2bf2b9cc48b6`, and
the cumulative appcast SHA-256 is
`7f2e13e4057e3fb2337eb2348bdb04affde3b088b188fd2b828bf5a3bf74c603`.

## Reviewed Release-Note Seed

> BD_to_AVP `v0.3.0-rc.2` improves recovery from malformed PGS subtitles and
> makes custom profiles retain Files & Recovery settings. Unusable subtitle
> tracks no longer abort a conversion, while profile pipeline, recovery, output,
> encoder, and activity choices now round-trip correctly.

## Qualification and Follow-Up

RC 2 passed release identity, signature, notarization, Gatekeeper, startup,
bundled-tool, profile persistence, and RC1-to-RC2 Sparkle-note qualification.
The exact problematic source in #458 still failed after RC 2, exposing a broader
malformed-PGS class. PRs #467 and #468 address that class and add aggregate,
privacy-safe subtitle outcomes; RC 3 must qualify those changes on its exact
signed artifact before #458 can close.

RC 2's run-bound signing approval does not authorize RC 3. Every later release
must repeat the fixed-`main`, watcher, and exact-run approval contract.
