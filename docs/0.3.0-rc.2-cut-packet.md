# 0.3.0 RC 2 Cut Packet

> **Prepared metadata; publication pending.** RC 2 may be dispatched only from
> the exact merged protected-`main` SHA under a temporary merge hold. The
> `macos-signing` environment requires fresh explicit run-bound authorization in
> the active conversation.

RC 2 follows immutable RC 1 and packages the malformed-PGS recovery hardening
for #458 from PR #463 and Files & Recovery profile persistence for #462 from PR
#464.

## Exact Metadata

| Field | Reviewed value |
| --- | --- |
| Package and bundle short version | `0.3.0rc2` |
| Public version | `0.3.0-rc.2` |
| Bundle and Sparkle build | `159` |
| GitHub tag and release title | `v0.3.0-rc.2` |
| DMG name | `3D-Blu-ray-to-Vision-Pro-0.3.0-rc.2.dmg` |
| Sparkle channel | `rc` |
| GitHub prerelease | `true` |
| GitHub Latest | `false` |
| Publish to PyPI or Homebrew | `false` |
| Privacy contract | Privacy rules version `5` |

Build `159` is globally newer than immutable RC 1 build `158`, abandoned Beta
12 build `157`, and burned builds `147` and `154`. RC 2 has no tag, release,
draft, appcast item, or freeze entry until guarded publication succeeds.

## Reviewed Release-Note Seed

> BD_to_AVP `v0.3.0-rc.2` improves recovery from malformed PGS subtitles and
> makes custom profiles retain Files & Recovery settings. Unusable subtitle
> tracks no longer abort a conversion, while profile pipeline, recovery, output,
> encoder, and activity choices now round-trip correctly.

## Qualification and Authorization

The last exact public artifact is immutable RC 1. RC 2 must freshly bind the
final protected-`main` SHA to its signed and notarized DMG, appcast identity,
RC update route, native notes, malformed-PGS recovery, and Files & Recovery
profile persistence. Existing RC 1 receipts are inputs only.

Before dispatch, record the exact protected `main` SHA and establish a temporary
merge hold. Monitor only with `uv run python -m scripts.github_release_run
watch`. If it exits `20`, obtain fresh explicit run-bound authorization in the
active conversation and approve only through `scripts.github_release_run
approve` using the emitted run, workflow, full `main` SHA, confirmation SHA,
and approval fingerprint.
