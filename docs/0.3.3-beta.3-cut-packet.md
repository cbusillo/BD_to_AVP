# 0.3.3 Beta 3 Cut Packet

**Published and immutable.**

## Candidate Identity

- Owner: issue #766.
- Package version: `0.3.3b3`.
- Public version: `0.3.3-beta.3`.
- Tag and release name: `v0.3.3-beta.3`.
- Build: `174`.
- Route: `Prerelease` / Sparkle `beta`; GitHub prerelease; not Latest; PyPI and
  Homebrew publication disabled.

This candidate adds conversion stall feedback, acknowledged Keep Waiting and
Stop controls, bounded wait extensions, and additional diagnostics. Activity
tracking distinguishes growing output from repeated log messages. The existing
unattended timeout and retry behavior remains in place.

The original reported MKV is not available for reproduction. This release does
not include remux recovery or claim to repair that file. Issue #759 tracks the
remaining experiment. Movie Sharing remains compatible with **Shiny 3D Cinema
0.1.0 (3)**; see the [Movie Sharing beta quick start](movie-sharing-beta.md).

Post-dispatch source SHA, signed DMG and app-tree checksums, release run, release
ID and appcast checksum remain unset in preregistration. Record those facts only
from the guarded release's immutable receipts.

## Immutable Prior

Published and qualified Beta `0.3.3b2` / build `173` is the Beta updater source.
Its immutable [release receipt](release-evidence/v0.3.3-beta.2/release-receipt.json)
and Release Evidence v2 record bind source
`619479750b2d6be88fc1173b01226908509eae0f`, Prerelease run `35140273939`
attempt `1`, and release ID `390229092`.

Stable `0.3.2` / build `171` remains the prior Stable. Burned builds `147`,
`154`, `165`, `166` and `168`, plus abandoned metadata build `157`, remain
unavailable for reuse. Prior releases and their evidence must remain unchanged.

## Production Preflight

Production Preflight run `35176062839`, attempt `1`, succeeded on exact
protected main `d1e64032683f71c3a7d828af74fa6ad61a3231da` before candidate
preparation. Both the actor and triggering actor were `shiny-code-bot`.

The retained artifact is `10478369302`, named
`production-preflight-success-d1e64032683f71c3a7d828af74fa6ad61a3231da-35176062839-1`,
with GitHub digest
`d24abca861a8a9c84381a37d8889f392ab18cfeacd1ca4fd34aaa119664657e6`.
The production package, embedded worker identity/protocol, preventive DMG,
installed profile persistence and update controls passed. These are preventive
results for `d1e64032683f71c3a7d828af74fa6ad61a3231da` only. The guarded
release engine runs the same preflight again on the final protected-main
dispatch SHA before signing; signed Beta 3 evidence comes from that release run.

## Qualification And Delivery

`docs/qualification/v0.3.3-beta.3-signed-qualification-v1.json` preregisters the
closed 20-case matrix, worker protocol 13 and the unchanged route-table digest.
The fresh automated updater case is Beta 2 to Beta 3. Preparation classification
requires fresh evidence for preview with low local capacity and ample destination
capacity, preview cancellation/failure cleanup, known-low capacity, unknown or
conflicting capacity, generated final output, overwrite/conversion cancellation,
and subtitle partial-output diagnostics. Collect those eight cases on the
prepared source through the documented `qualify/v0.3.3-beta.3` branch before
Prerelease dispatch. Malformed PGS parser recovery retains its valid prior
evidence under the unchanged policy.

The classifier reads accepted functional receipts from the append-only
`docs/qualification/release-evidence-v1.json` index. Release Evidence v2 is the
separate immutable publication/qualification bundle; the Beta 2 receipts already
appear in the functional index, so no predecessor evidence is relabeled.
Reclassify the final protected-main SHA after the evidence merge to verify that
the evidence-only delta preserves these results. The optional native notes case
is classified for fresh retest; prior presentation evidence does not cover this
candidate.

The candidate tag and authenticated release/draft listing were checked absent
before preparation. Recheck absence and exact protected main before dispatch.
Keep main fixed while the release is nonterminal. Monitor with
`uv run python -m scripts.github_release_run watch`; exit `20` requires the
repository's explicit run-bound signing authorization. Approve only with the
helper's exact run ID, workflow, full main SHA, confirmation SHA and fingerprint.

After publication, verify the signed updater path and public download, then
reconcile immutable Release Evidence and milestone results through protected
pull requests. Update the TestFlight Mac download wording through the existing
App Store Connect API route. Broader measured headset work remains in #739.

<!-- release-evidence-receipt-v1 -->
## Immutable Publication Receipt

- Source SHA: `ca30b2692c92b13609e8402cd355187a4551b81d`
- Release workflow run: `35183646485` attempt `1`
- GitHub release ID: `390452517`
- Published at: `2026-09-17T05:23:56Z`
- DMG SHA-256: `21e176f80ca6d6c520c0966ff8b3aff99e0bd7004046bd4db2a63f67c860da92`
- Signed app-tree SHA-256: `e5e672ee446c3a44a1a42fcd69d8b3293532283deac0d359499014e754a17b2f`
- Appcast SHA-256: `5fb8e2ea5362c8750153e6f4755deaa6049421f0b47d32d1265eb88890b88969`
- Receipt file SHA-256: `c11e701ee0defaf773e97e64bddbb99b8d44a71db46478ab2e3b4c657f020fff`
